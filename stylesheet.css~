@charset "utf-8";

body {
  background: #efefef;
}

.main {
  position: relative;
  margin: auto;
}

h1 {
  position: absolute;
  top: 1%;
  left: 38%;
  font-size: 40px;
  -ms-transform: translate(-50%,-50%);
  -webkit-transform: translate(-50%,-50%);
  transform: translate(-50%,-50%);
  margin:0;
  padding:0;
  display: none;
}

img {
  width: 1420px;
  height: 1000px;
  display: none;
}

#btn {
  margin: 30px auto 0;
  width: 200px;
  height: 200px;
  border-radius: 50%;
  background: #00bfff;
  text-align: center;
  line-height: 200px;
  color: #fff;
  font-size: 42px;
  font-weight: bold;
  cursor: pointer;
  opacity: 0.9;
  box-shadow: 0 10px 0 #1e90ff;
  border: 3px solid #1e90ff;
  user-select: none;
}

#btn:hover {
  opacity: 1;
}

#btn.pushed {
  margin-top: 36px;
  box-shadow: 0 4px 0 #1e90ff;
}
